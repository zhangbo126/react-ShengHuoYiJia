export default(state,action)=>{

     state = state || {home:'32121'}
     // eslint-disable-next-line default-case
     switch(action.type){
         case 'init':
            return{}
          default:
        return{}  
     }

}